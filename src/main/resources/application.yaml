spring:
  application:
    name: jdbcgraphql

  
  # Enabling graphql playground
  graphql:
    graphiql:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true

server:
  port: 9191
